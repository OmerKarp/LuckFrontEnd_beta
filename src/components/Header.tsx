import { Box, Typography } from "@mui/material";

const Header = () => {
  return (
    <div>
      <Typography variant="h1" color="primary">
        Elements Lottery
      </Typography>
    </div>
  );
};

export default Header;
